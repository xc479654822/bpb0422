name: Test Permissions

on: [push]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Print message
        run: echo "✅ GitHub Actions 有运行权限"
